<h4>{{ _('Julia 2018 Filament Sensor') }}</h4>
<form class="form-horizontal" id="settings_j18fs">
    <div class="control-group">
        <label class="control-label">{{ _('Pin 1:') }}</label>
        <div class="controls" data-toggle="tooltip" title="{{ _('Which Raspberry Pi GPIO pin your switch is attached to (-1 disables the plugin)') }}">
            <input type="number" step="any" min="-1" class="input-mini text-right" data-bind="value: config.pin">
        </div>
    </div>
    <div class="control-group">
        <label class="control-label">{{ _('Pin 1 Debounce Time:') }}</label>
        <div class="controls" data-toggle="tooltip" title="{{ _('When a sensor state change happens, wait for this amount of time to wait for the signal to stabilize.') }}">
            <input type="number" step="any" min="0" class="input-mini text-right" data-bind="value: config.bounce">
            <span class="add-on">ms</span>
        </div>
    </div>
    <div class="control-group">
        <label class="control-label">{{ _('Pin 1 Switch Type:') }}</label>
        <div class="controls" data-toggle="tooltip" title="{{ _('Whether the sensor should trip when the switch goes HIGH or LOW, Normally Open is LOW when filament is present, and Normally Closed is HIGH when filament is present.') }}">
            <select class="select-mini" data-bind="value: config.switch">
                <!-- value - state when filament is present -->
                <option value=0>{{ _('Normally Closed') }}</option>
                <option value=1>{{ _('Normally Open') }}</option>
            </select>
        </div>
    </div>
    <div class="control-group">
        <label class="control-label">{{ _('Out of filament GCODE:') }}</label>
        <div class="controls">
            <textarea rows="4" cols="2" class="block" data-bind="value: config.gcode_pin"></textarea>
        </div>
    </div>

    <hr/>
    
    <div class="control-group">
        <label class="control-label">{{ _('Pin 2:') }}</label>
        <div class="controls" data-toggle="tooltip" title="{{ _('Which Raspberry Pi GPIO pin your switch is attached to (-1 disables the plugin)') }}">
            <input type="number" step="any" min="-1" class="input-mini text-right" data-bind="value: config.pin2">
        </div>
    </div>
    <div class="control-group">
        <label class="control-label">{{ _('Pin 2 Debounce Time:') }}</label>
        <div class="controls" data-toggle="tooltip" title="{{ _('When a sensor state change happens, wait for this amount of time to wait for the signal to stabilize.') }}">
            <input type="number" step="any" min="0" class="input-mini text-right" data-bind="value: config.bounce2">
            <span class="add-on">ms</span>
        </div>
    </div>
    <div class="control-group">
        <label class="control-label">{{ _('Pin 2 Switch Type:') }}</label>
        <div class="controls" data-toggle="tooltip" title="{{ _('Whether the sensor should trip when the switch goes HIGH or LOW, Normally Open is LOW when filament is present, and Normally Closed is HIGH when filament is present.') }}">
            <select class="select-mini" data-bind="value: config.switch2">
                <!-- value - state when filament is present -->
                <option value=0>{{ _('Normally Closed') }}</option>
                <option value=1>{{ _('Normally Open') }}</option>
            </select>
        </div>
    </div>
    <div class="control-group">
        <label class="control-label">{{ _('Out of filament 2 GCODE:') }}</label>
        <div class="controls">
            <textarea rows="4" cols="2" class="block" data-bind="value: config.gcode_pin2"></textarea>
        </div>
    </div>
    
    <hr/>

    <div class="control-group">
        <label class="control-label">{{ _('Board Pin Mode:') }}</label>
        <div class="controls" data-toggle="tooltip" title="{{ _('RPi pins numbered in Board mode or BCM mode?') }}">
           <select class="select-mini" data-bind="value: config.mode">
                <option value=0>{{ _('Board Mode') }}</option>
                <option value=1>{{ _('BCM Mode') }}</option>
        	</select>
        </div>
    </div>
    <div class="control-group">
        <div class="controls" data-toggle="tooltip" title="{{ _('Beware that for some printers, pausing is incompatible with M600 (Filament change pause) as both will move the print head and it can resume at a different position as it paused.') }}">
            <label class="checkbox">
                <input type="checkbox" data-bind="checked: config.pause_print"> {{ _('Pause print when out of filament') }}
            </label>
        </div>
    </div>
	
    <div class="control-group">
        <div class="controls">
            <button class="btn btn-wrning" data-bind="click: testStatus">Test Status</button>
        </div>
    </div>
</form>